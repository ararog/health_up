[project]
name = "health-up"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "fastapi[standard]>=0.115.8",
  "kafka-python>=2.0.5",
  "python-decouple>=3.8",
]
